export const READ_JOURNAL_INSTRUCTIONS = `## Tool: readJournal
Use this tool to read the user's journal entry for a specific date.

When to use:
- When the user asks about what they wrote or reflected on a particular day
- When the user references a past journal entry
- When you need context about the user's reflections on a specific date

Tips:
- Always specify the exact date in YYYY-MM-DD format
- Do not read journal entries without the user mentioning or implying a specific date
- Summarize or reference the content naturally in conversation
- If no entry exists for the requested date, let the user know gently
`;
